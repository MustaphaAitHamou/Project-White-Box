/* eslint-env node */
/* global module */

/**
 * Je crée ici un stub pour tous les imports de styles (CSS/SCSS…) utilisés
 * dans les tests. Dans Jest, tenter d’importer un .css déclencherait une
 * erreur de syntaxe, donc je renvoie simplement un module vide.
 * Cela suffit à calmer Jest sans perturber l’application.
 */

// Jest mock pour les imports de styles.
module.exports = {};
